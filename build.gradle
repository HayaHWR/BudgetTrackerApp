buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Aktualisiere auf eine neuere Version, die mit deiner Gradle-Version kompatibel ist
        classpath 'com.android.tools.build:gradle:7.4.2'
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

// WICHTIG: ENTFERNE DEN ALLPROJECTS BLOCK KOMPLETT!
// Der allprojects Block verursacht den Fehler, weil in settings.gradle
// repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) gesetzt ist

final def task = task clean(type: Delete) {
    delete rootProject.buildDir
}
